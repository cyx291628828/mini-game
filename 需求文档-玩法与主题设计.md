# 需求文档 · 玩法模式与主题设计

> 项目：休闲益智游戏合集（数独 / 杀手数独 / 数方 / 星之战 / 扫雷）
> 版本：v0.3　日期：2026-09-15　配套：《技术方案》《README（决策日志）》《config/（配置表）》

---

## 1. 决策记录（Decision Log）

| 日期 | 决策 | 状态 |
|---|---|---|
| 2026-09-15 | 技术栈：TypeScript + Cocos Creator 3.8，开发模式为"骨架代码化 + 编辑器精修"混合 | ✅ 已定 |
| 2026-09-15 | 变现：IAA 纯广告（激励视频为主），微信首发、抖音次之 | ✅ 已定 |
| 2026-09-15 | 主线玩法：大富翁式棋盘推进（用户提案） | ✅ 已定 |
| 2026-09-15 | 元游戏：数据驱动"配置表 + MetaEngine 逻辑 + 皮肤表现"三层 | ✅ 已定 |
| 2026-09-15 | 「谜题大富翁 × 拼图之门」章节制：节点挑战结算掉落下一章拼图碎片；未集齐不解锁下一章；锁定态到轨道末端反弹回走；集齐拼合后只能正向走直至进入下一章 | ✅ 已定 |
| 2026-09-15 | **落格后可选玩法难度，按"玩法 × 难度"奖励表结算；移除 Boss 格与顶点格**（轨道末端边界承担顶点反弹 / 章节门职能）；**奖励、轨道、全局参数全部配置表化**（`config/*.csv`，人工可编辑） | ✅ 已定 |
| — | 每日挑战采用"益智之塔"爬塔制 | 🔶 倾向，待定 |
| — | 游戏命名、首发章节数等 | ⬜ 待定（见 §7） |

---

## 2. 主线模式：「谜题大富翁 × 拼图之门」章节制

### 2.1 一句话

每章是一张**线性大富翁轨道**；落格触发该节点玩法、**玩家自选难度**、通关按配置表结算并掉落**下一章拼图碎片**；拼图集齐拼出完整图案 → 只能正向前进，**越过轨道末端进入下一章**。

### 2.2 章节结构与状态机（核心规则，已定稿）

```
章节 N = 轨道棋盘（起点 → 轨道末端，线性，格数在配置表）
       + 门禁拼图（图案为"章节 N+1"的章节图，碎片在章节 N 掉落收集）
       + 章节结算仪式（拼合动画 + 奖励）
```

**棋子只有两种状态：**

| 状态 | 条件 | 移动规则 |
|---|---|---|
| 🔒 **锁定态** | 下一章拼图未集齐 | 沿轨道**往返**：正向越过**轨道末端 → 反弹回走**；走回起点 → 再反弹正向（两端都是边界墙，受全局参数开关控制）。往返中照常触发格子、点亮、掉碎片 |
| 🔓 **解锁态** | 拼图集齐且拼合完成 | 方向**锁定为正向，只能前进**；越过轨道末端即触发章节结算、进入下一章；期间一切"后退"效果（传送等）转为前进（开关控制） |

- 章节 1 开局直接进入；章节 N+1 的碎片在章节 N 内掉落。
- 锁定态的往返 = **刷碎片赛季**：骰子持续消耗 → 广告补充，天然 IAA 节奏。
- 无 Boss 格、无顶点格（v0.3 定稿移除）：轨道末端**边界**本身承担顶点反弹与章节门职能；高难挑战由"任意节点自选专家难度"替代。

### 2.3 难度选择机制（v0.3 新增，已定稿）

- 落在玩法格 → 弹出**难度选择面板**：简单 / 中等 / 困难 / 专家四档，**每档显示奖励预览**（读配置表）→ 选定开局。
- **默认全档开放**（含专家），风险自担：高难失败 = 浪费骰子只拿安慰金；如需"按章节解锁难度档"为待定项。
- 不同难度 → 不同谜题规格 + 不同奖励，全部由 `config/难度奖励表.csv` 配置（见 §3）。
- 失败：原地不动，只发该档"失败安慰金币"，不计通关、不掉碎片。
- 重试（看广告原地重试）：沿用所选难度重来。
- 重复通关：按所选难度**全额**结算（进度由骰子消耗自然限速，不再额外减半）；碎片受掉率与保底约束。

### 2.4 拼图碎片经济

| 获取途径 | 掉落 | 配置位置 |
|---|---|---|
| 玩法格**首次点亮** | 额外**必掉 1 片缺片** | 全局参数「首通点亮必掉碎片」 |
| 通关（任意难度） | 必得碎片 + 概率碎片（难度越高越多） | 难度奖励表「拼图碎片必得 / 额外碎片掉率%」 |
| 幸运转盘 / 成就 / 签到 / 每日挑战 | 少量补充 | 各自配置 |
| 激励视频"碎片加速" | 每日限 2 次额外得 1 片，标注"可免费获得" | 全局参数「碎片视频加速每日次数」 |

- **保底**：连续 3 次结算无新碎片 → 下次必掉缺片（全局参数「碎片保底阈值」）。免费路径必须顺畅。
- **合规红线**：拼图是章节门禁，广告碎片只能加速、**绝不能成为必要条件**（激励视频不得作为关卡解锁前置）。
- **溢出转化**：本张拼图集齐后碎片掉落自动转金币。
- **拼图馆**：拼合完成的图进入图鉴页；收集中的拼图显示缺片轮廓。

### 2.5 节奏设计（数值均在配置表，可直接调）

- 首章 24 格、下一章拼图 9 片 → 约 1.5–2 个往返集齐，单章目标 3–5 天。
- 片数随章节递增（9 → 12 → …），轨道同步加长。

### 2.6 章节结算仪式（动效重点）

越过轨道末端（解锁态）→ **拼合动画**（碎片飞入、全图点亮，编辑器精修重点）→ 章节奖励结算页 → 新章节开场（新皮肤 + 新玩法解锁引导）。

### 2.7 格子类型（v0.3 定稿，配置于轨道表）

| 格子类型 | 行为 |
|---|---|
| 起点格 | 锁定态反弹墙；经过领小额补给（全局参数） |
| **玩法格**（核心） | 落格 → 选难度 → 开局；首通点亮 + 必掉缺片；按难度结算 |
| 金币格 | 直接获得（全局参数「金币格金额」） |
| 骰子格 | +1 骰子 |
| 事件格 | 幸运转盘 / 下格奖励双倍 / 传送（锁定态可后退，解锁态转前进） |

~~Boss 格、顶点格~~：v0.3 移除（高难挑战 = 任意节点选专家难度；顶点职能 = 轨道末端边界）。

### 2.8 玩法解锁顺序（新手曲线）

| 章节 | 解锁玩法 | 理由 |
|---|---|---|
| 章节 1 | 数独（4×4→6×6）+ 扫雷（小盘） | 认知度 + 零学习成本双主角 |
| 章节 2 | 数方 | 规则简单、视觉新颖 |
| 章节 3 | 星之战 | 放在习惯建立后 |
| 章节 4+ | 杀手数独 + 混合格子 | 最难收尾 + 保持新鲜 |

### 2.9 收集、社交与广告位

- 收集：拼图馆（核心）、成就（累计通关/无提示通关/连击/馆藏数）、7 日签到；好友排行（章节进度 + 馆藏数）。
- 广告：激励视频 = 补骰子 / 碎片加速 / 失败重试 / 结算双倍 / 转盘次数；插屏 = 章节结算后、返回首页（频控）；原生模板 = 首页/棋盘页底部。

---

## 3. 配置表体系（v0.3 新增，人工可编辑）

**位置：`config/`，直接用 Excel / 文本编辑器编辑，游戏加载时自动读取并校验。**

| 文件 | 职责 |
|---|---|
| `难度奖励表.csv` | **玩法类型 × 难度 → 奖励**（金币/碎片必得/碎片掉率/骰子/首通加成/失败安慰金）+ 每档谜题规格。当前 5 玩法 × 4 难度 = 20 行 |
| `章节N-轨道表.csv` | 每个节点：格子类型 / 玩法类型 / 参数事件。首章 24 节点已配好（玩法格占 50%，数独/扫雷交替） |
| `全局参数表.csv` | 骰子经济、碎片保底、反弹开关、金币格金额、章节数等键值对 |
| `README-配置说明.md` | 字段字典、合法枚举、结算公式、编辑注意（勿改表头/枚举） |

结算公式（代码按此实现，详见 config/README）：通关 = 表行全额（首通叠加首通加成 + 必掉 1 片缺片）；碎片 = 必得 + 掉率判定，受保底兜底；失败 = 仅安慰金。

## 4. 元游戏架构：配置表 + MetaEngine + 皮肤

```
config/*.csv（配置层）   轨道、奖励、全局参数 —— 人工编辑
MetaEngine（逻辑层）     章节状态机（锁定/解锁）、移动与边界反弹、落格触发、难度选择、奖励结算、碎片账本与保底
BoardView（表现层）      轨道皮肤（线性大富翁盘 / 未来换皮）
```

存档核心字段：章节号、轨道位置、移动方向、各格点亮状态、当前拼图碎片集合、拼图馆、骰子/金币。
加玩法 = 玩法模块 + 难度奖励表加 4 行 + 轨道表使用；加章节 = 复制轨道表 + 登记参数，主线代码不动。

## 5. 主题路线

- ✅ 拼图修复已并入主线（章节门禁 + 拼图馆）。
- **益智之塔** → 每日挑战首选（待定稿）；环球旅行家 → 换皮候选；侦探事务所 → 中期活动；星际航线 → 太空皮肤；浮空岛复苏 → 远期看数据。

## 6. 与技术方案的衔接

- MetaEngine（含难度奖励结算、碎片保底）为纯逻辑模块，单测覆盖；里程碑 M1–M2。
- 配置加载器：CSV 解析 + 校验（枚举、行数、概率范围），报错带文件名与行号。
- 拼图资源管线：AI 生成章节整图 → 切片 → 缺片轮廓自动生成。

## 7. 待定项

1. 难度档是否按章节解锁（当前默认全开放）。
2. 首发章节数（配置表当前按 3 章登记）。
3. 每日挑战定稿为益智之塔？
4. 游戏命名（谜题乐园 / 谜题大亨 / 益智嘉年华 / 谜境岛…），需查重名与商标。
5. 是否加轻剧情（事件格文案风格）。
6. 各项数值为建议初值，你可直接改 `config/` 配置表调整，无需走文档流程；改"规则"（格子类型、状态机）才需要更新本文档。
