{
  "name": "puzzle-monopoly",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "config": "node scripts/sync-config.mjs",
    "dev": "node scripts/sync-config.mjs && vite",
    "build": "node scripts/sync-config.mjs && tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.3.5"
  }
}
