# 休闲益智游戏合集 · 技术与产品方案

> 数独 / 杀手数独 / 数方 / 星之战 / 扫雷 —— 多玩法休闲合集，主打微信、抖音等小游戏平台，纯广告变现（IAA）
> 日期：2026-09-15

---

## 1. 结论先行（TL;DR）

| 决策项 | 选择 | 理由一句话 |
|---|---|---|
| **开发语言** | **TypeScript** | 强类型适合多玩法中型项目，与引擎深度绑定 |
| **游戏引擎** | **Cocos Creator 3.8.x（LTS）** | 国内小游戏市占率最高，构建面板一键发布微信/抖音/支付宝/QQ/百度/各快游戏平台 |
| **游戏形态** | 小游戏（非小程序页面） | 平台要求游戏以"小游戏"类目发布，小游戏环境只有 Canvas/WebGL、无 DOM，必须用游戏引擎 |
| **变现模式** | IAA 纯广告（激励视频为主 + 插屏） | 免版号，个人主体即可上线；内购(IAP)才需要版号，暂不碰 |
| **首发平台** | 微信小游戏 → 抖音小游戏 → 支付宝/QQ/快游戏 | 微信流量最大、审核资料相对最少；抖音资质要求略高 |
| **后端** | 无自研后端（0 服务器成本） | 进度存本地；每日挑战用日期做随机种子，天然全平台一致；排行榜用平台自带好友排行 |

**一句话：用 TypeScript + Cocos Creator 做"一个壳 + 五个玩法模块"的单包合集，全程无后端，先微信后抖音铺量，靠激励视频广告赚钱。**

---

## 2. 技术选型调研

### 2.1 候选方案对比

| 方案 | 语言 | 多平台发布 | UI/动画能力 | 包体/性能 | 生态与风险 | 结论 |
|---|---|---|---|---|---|---|
| **Cocos Creator 3.8** | TypeScript | 构建面板直发微信/抖音/支付宝/QQ/百度/华为/OPPO/vivo/小米 | 内置 UI 系统 + 动画编辑器 + Tween + 粒子 + 龙骨/Spine | 2D 性能好，引擎模块可裁剪，首包易控 4MB 内 | 中文文档/社区最全，国内小游戏占有率第一，免费 | ✅ **推荐** |
| LayaAir 3 | TypeScript | 同样支持各小游戏平台 | UI/动画可用，相对偏重 3D | 2D 性能好 | 社区与案例量级明显小于 Cocos，部分高级功能收费 | 备选 |
| Unity（微信转换方案）/ 团结引擎 | C# | 需专门的转换/导出链路 | 强，但面向 3D 重度 | 转换后包体数十 MB、启动慢，审核风险高 | 对回合制益智游戏是杀鸡用牛刀 | ❌ 不推荐 |
| 纯 JS + Canvas 自研 | JavaScript | 各平台 API 差异全要自己适配 | UI/动画/适配/音频全自研 | 最小 | 开发与长期维护成本最高，动画效果难做"绚丽" | ❌ 不推荐 |
| 微信小程序页面（DOM/WXML）+ 嵌 Canvas | JS | 仅微信生态，抖音要另写一套 | WXML 动画有限 | 小 | 游戏必须走"小游戏"类目，此路在抖音等平台不通 | ❌ 不推荐 |
| Egret 白鹭 | TS/JS | — | — | — | 引擎已基本停止维护 | ❌ 淘汰 |

### 2.2 选 Cocos Creator 的核心理由

1. **一键发布**：编辑器"构建发布"面板直接产出微信小游戏、抖音小游戏等目标包，自动处理 Canvas 适配、触摸事件、生命周期差异；抖音 API（`tt.*`）与微信（`wx.*`）高度兼容，适配成本低。
2. **UI 与动画正是强项**：内置 Button/ScrollView/Layout/ProgressBar 等控件体系、Animation 编辑器做关键帧动画、`tween` 做缓动、粒子系统做特效——满足"UI 交互 + 绚丽动画"的诉求，不需要自己造轮子。
3. **TypeScript 原生开发**：谜题生成器/求解器是纯逻辑代码，可完全脱离引擎编写和单元测试，复用性和可维护性好。
4. **包体可控**：2D 项目裁掉 3D/物理等模块后，引擎 + 首包资源可以压进微信首包 4MB 限制；玩法内容走分包（总包上限 30MB）。
5. **成熟的广告接入路径**：两个平台的广告 SDK（微信流量主、穿山甲）在 Cocos 生态里有大量现成封装案例。

---

## 3. 平台与资质合规（重要，尽早启动）

> 软著办理周期最长（正常 1~2 个月），**建议项目第一周就提交申请**，与开发并行。

| 平台 | 上线资质（纯广告 IAA） | 广告产品 | 备注 |
|---|---|---|---|
| **微信小游戏** | 软著（或电子版权认证证书）+ 游戏自审自查报告 + 备案 | 流量主：激励视频 / 插屏 / 原生模板（横幅、格子） | 流量主开通条件：累计独立访客 **UV > 500** 且无违规；激励视频 eCPM 官方口径 > 50 元，是主力变现组件 |
| **抖音小游戏** | **必须软著**（用于备案需国家版权局正式登记证书，电子软著只能做准入）+ ICP 备案 + 身份证/银行卡等材料 | 穿山甲（激励视频、插屏、信息流） | 软著著作权人须与账号主体一致；不支持代理接入 |
| 支付宝 / QQ / 百度小游戏 | 各自类目资质，均需软著 | 各自广告组件 | 第二梯队铺量 |
| 华为 / OPPO / vivo / 小米 快游戏 | 快应用资质 + 软著 | 各自联盟广告 | Cocos 同样一键发布，后期低成本移植 |

**红线提醒（两家平台共通，违规会被下架/封流量主资格）：**
- 不能把"看激励视频"做成玩游戏的强制前置（例如：不看广告就解锁不了关卡）；
- 不能无用户点击就触发激励视频、不能诱导误点；
- 涉及内购/虚拟支付才需要**版号**——本项目走纯广告，**不需要版号**。
- 主体建议：有公司资质走企业主体（后续商业化空间更大）；个人主体也可上线 IAA 小游戏。

---

## 4. 产品方案

### 4.1 玩法合集（首版 5 个）

| 玩法 | 英文/通用名 | 规格与难度 | 技术要点 |
|---|---|---|---|
| 数独 | Sudoku | 4×4 / 6×6 / 9×9；入门→专家 5 档 | 随机满盘生成 + 挖洞保证**唯一解**（回溯求解器计数剪枝），按技巧等级（唯一候选/区块/数对/X-Wing…）评级难度 |
| 杀手数独 | Killer Sudoku | 9×9，虚线笼 cage | 数独约束 + cage 和约束；生成：先出数独解盘，再按和值贪心切分 cage，求解器做 cage 内组合枚举 |
| 数方 | Suguru / Tectonics | 6×6 / 8×10 不规则区块，数字 1..区域大小 | 随机区域生长生成拼块 + 回溯填数保证唯一解；视觉上不规则区域是亮点 |
| 星之战 | Star Battle | 8×8 / 10×10，每行/列/区域放 2（或 3）颗星且互不相邻 | 随机区域划分 + 回溯放置 + 唯一解校验；点击切换 空/星/排除 三态 |
| 扫雷 | Minesweeper | 9×9/10雷 → 16×30/99雷 | 首点必安全（首点后再生雷），支持双击快开、标记模式切换；3BV 评估盘面强度 |

**每个玩法的统一能力**：铅笔笔记（候选数标注）、撤销/重做、提示（看广告）、错误高亮（可关）、计时、暂停、完成庆祝动画、错误次数/步数结算。

### 4.2 系统层（壳功能）

- **首页**：玩法卡片宫格 + 每日挑战入口 + 签到/成就入口；卡片式 UI 配入场动效。
- **主线模式（已定稿，详见《需求文档-玩法与主题设计.md》）**：「谜题大富翁 × 拼图之门」章节制——线性轨道掷骰走格、落格后玩家自选玩法难度、按"玩法×难度"奖励配置表结算、通关掉落下一章拼图碎片；拼图未集齐时轨道末端反弹回走（锁定态往返），集齐拼合后只能正向走、越过末端进入下一章（解锁态）。**棋盘轨道/奖励/全局参数全部由 `config/*.csv` 配置表驱动**（人工可编辑），实现 配置 + MetaEngine（章节状态机/碎片账本）+ 皮肤 三层分离。
- **每日挑战**：用 `日期字符串做随机种子` 生成谜题——所有玩家同一天拿到同一题，**无需服务器**，配合排行榜做社交传播。
- **道具经济**：金币（看广告、签到、成就获得）→ 换提示/跳过；营造"看广告换奖励"的正循环（IAA 核心模型）。
- **成就 + 平台好友排行榜**（微信/抖音自带社交组件），提升留存。
- **设置**：音效/音乐、震动、错误提示、左侧利手、深色模式、多语言（中/英，利于出海复用）。

### 4.3 广告位设计（IAA 变现模型）

| 广告类型 | 触发点 | 频控 |
|---|---|---|
| **激励视频**（主力） | 获取提示、撤销包、扫雷复活、结算双倍金币、每日挑战额外次数、跳过本关 | 用户主动触发，无限次入口但奖励边际递减 |
| **插屏** | 关卡结算页之后返回首页时 | 每 ≥3 关展示 1 次，避免打扰 |
| 原生模板/横幅 | 首页底部、关卡选择页底部 | 固定位置，不遮挡棋盘 |

> 设计原则：广告只换"增益"，不卡"进度"，既守住审核红线，也不伤留存。

### 4.4 UI / 动画规格（"绚丽"落到具体处）

- **全局**：主题化配色（至少 3 套皮肤：清新蓝 / 暗夜 / 樱花），统一的圆角卡片 + 毛玻璃质感面板；场景转场用缩放淡入 + 模糊。
- **棋盘动效**：开局格子逐个弹入（stagger 弹跳）；填对数字弹性缩放 + 粒子迸发；填错红色抖动 + 震屏；行列/区域完成时扫光高亮。
- **庆祝**：通关彩带粒子 + 连击数字滚动 + 星级逐颗点亮 + 金币飞向钱包图标（tween 路径动画）。
- **交互反馈**：全按钮按压缩放、选中格呼吸光效、笔记数字淡入、拖尾连消粒子。
- 实现：Cocos `tween` 缓动链 + Animation 编辑器关键帧 + 2D 粒子；音频用引擎 AudioEngine（按键音/完成音/庆祝音分层）。

---

## 5. 技术架构

```
PuzzlePalace（合集壳）
├─ core/                       # 平台无关的核心服务
│   ├─ SceneRouter            # 场景管理/转场
│   ├─ AudioMgr / HapticMgr   # 音频、震动（平台差异封装）
│   ├─ Storage                # 存档封装（JSON + 版本迁移）
│   ├─ AdAdapter              # 广告抽象层：wx 流量主 / 抖音穿山甲 同一接口
│   ├─ Platform               # wx.* / tt.* API 差异抹平层
│   └─ Economy / Quest        # 金币、任务、成就
├─ puzzle-kit/                 # ★ 纯 TS 谜题算法库（零渲染依赖，可跑 Node 单测）
│   ├─ sudoku/ killer-sudoku/ suguru/ star-battle/ minesweeper/
│   │   ├─ Generator（生成器，唯一解保证）
│   │   ├─ Solver（求解器，含难度评级）
│   │   └─ Types
│   └─ SeedRng                # 可播种随机数（每日挑战一致性）
├─ ui-kit/                     # 通用 UI 组件
│   ├─ GridBoard              # 统一格盘组件：选中/笔记/高亮/动效接口
│   ├─ Keypad / NotePad       # 数字键盘、笔记面板
│   ├─ Dialog / Toast / RewardFx
│   └─ ThemeMgr               # 皮肤系统
└─ games/                      # 每个玩法一个场景 + 控制器（分包）
    ├─ sudoku/  killer-sudoku/  suguru/  star-battle/  minesweeper/
```

设计要点：
- **算法与表现彻底分离**：`puzzle-kit` 不 import 任何引擎模块，用 Jest/Vitest 做生成器唯一解、难度分布的批量回归测试。
- **统一 GridBoard 抽象**：五个玩法全是"格子谜题"，棋盘渲染、输入、笔记、高亮、动画共用一套组件，每个玩法只写规则差异，**这是合集类项目控制成本的关键**。
- **存档零后端**：本地 JSON + schema 版本号迁移；每日挑战用日期种子。
- **平台抽象层**：登录、分享、排行榜、震动、广告全部走 `core/Platform` + `AdAdapter`，新平台只需实现一个 adapter。

### 包体与性能预算（以微信为准）

| 项目 | 预算 | 手段 |
|---|---|---|
| 首包（主包） | ≤ 4MB | 引擎裁剪（去 3D/物理/视频）、图集合并、纹理压缩（ETC2/ASTC）、音频低码率；首个玩法放首包 |
| 总包 | ≤ 30MB | 每个玩法一个分包，首次点击再下载（微信会缓存） |
| 帧率 | 中低端安卓稳定 60fps | 回合制 2D，UI 合批、避免每帧重排；无压力 |

---

## 6. 里程碑计划（约 10 周到微信首发）

| 阶段 | 时间 | 内容 |
|---|---|---|
| M0 | W1 | 工程搭建、core/ui-kit 框架、**软著提交申请**（并行）、平台开发者账号注册 |
| M1 | W2–3 | 数独全功能（生成/求解/笔记/提示/动画）+ 首页壳 —— 竖切样板 |
| M2 | W4 | 扫雷 + 关卡进度/存档 + 金币经济 |
| M3 | W5–6 | 数方 + 星之战（复用 GridBoard，速度加快） |
| M4 | W7 | 杀手数独 + 每日挑战 + 成就签到 |
| M5 | W8 | 动画/音效整体打磨、主题皮肤、新手引导 |
| M6 | W9 | 微信广告接入（激励视频/插屏）、真机性能与包体优化、内测 |
| M7 | W10 | 微信提审 → 发布；启动抖音备案与适配 |
| 后续 | 持续 | 抖音发布 → 支付宝/QQ → 快游戏平台；玩法扩展（未来：Nonogram 数织、Fillmat、点灯等，架构已预留） |

---

## 7. 风险与对策

| 风险 | 对策 |
|---|---|
| 软著周期卡上线 | 第一周就申请；微信可先用电子版权认证证书（10–15 天），抖音备案必须正式证书，排期前置 |
| 激励视频审核红线 | 广告只做增益不做门槛；严格按两平台《广告运营规范》设计触发点 |
| 首包超 4MB | 引擎模块裁剪 + 纹理压缩 + 玩法分包，构建时做包体监控 |
| 多平台 API 差异 | 统一 Platform/AdAdapter 抽象层，首发微信验证后再铺 |
| 同类竞品多 | 差异化靠：合集一站式 + 主题皮肤 + 每日挑战排行 + 动画品质；投放暂不投入，靠平台自然量+好友排行裂变 |

---

## 8. AI 主导开发的工作流适配（代码优先）

> 前提：代码大部分由 AI 生成，人工主要负责资源供给、审美把关与平台操作。

**结论：技术选型不变（TypeScript + Cocos Creator）；开发模式采用"骨架代码化 + 编辑器精修"的混合模式——代码负责一切动态内容与逻辑，编辑器负责一切"所见即所得"的视觉精修（人工操作编辑器，AI 出操作单辅助）。**

### 8.1 为什么选型不变

- **平台硬约束不变**：小游戏运行时无 DOM，最终产物仍是 Cocos→微信/抖音的构建链路，换任何框架都绕不开这一层。
- **AI 最强的语言恰是 TS/JS**：我们的开发语言本来就是 AI 训练语料最丰富的语言；而 Cocos 内置的 UI 控件、tween、粒子、平台适配是"久经考验的现成代码"——引擎越成熟，AI 需要从零发明的代码越少，出错面越小。
- **合集架构天然适合 AI 批量实现**：`puzzle-kit` 纯逻辑库（可全自动单测）+ `GridBoard` 统一抽象（一个接口、五个玩法），正是 AI 并行产出的理想形态。

### 8.2 工作流必须做的调整

1. **分工总表：什么用代码、什么用编辑器**

   | 事项 | 实现方式 | 负责方 | 理由 |
   |---|---|---|---|
   | 棋盘/格子/动态列表等运行时生成的 UI | 纯代码 | AI | 内容动态生成，编辑器拼不出来 |
   | 谜题算法、游戏逻辑、输入、存档、广告、平台适配 | 纯代码 | AI | 纯逻辑，与编辑器无关 |
   | 微交互（按压、弹入、抖动等小 tween） | 代码 tween | AI | 量大且琐碎，代码最可维护 |
   | 复杂编排动画（通关庆祝、转场、金币飞行等多节点时序动画） | **编辑器动画时间线** | **人工操作 + AI 操作单** | 关键帧缓动曲线可视化调试，效果上限更高 |
   | 粒子特效（彩带、扫光、迸发） | **编辑器粒子面板实时预览调参** | **人工操作 + AI 参数单** | 盲调代码粒子效率极低，编辑器所见即所得 |
   | 预制体布局微调、锚点对齐、九宫格图、皮肤材质 | **编辑器** | **人工操作 + AI 操作单** | 像素级视觉调整必须靠眼睛 |
   | 背景/装饰层构图 | 编辑器 | 人工操作 + AI 操作单 | 构图是审美问题 |

2. **编辑器产物的接线约定（混合模式的关键纪律）**
   - 统一命名规范：预制体 `fx_win_celebration.prefab`、动画剪辑 `anim_level_complete`、粒子 `fx_confetti.effect`——AI 从代码按名字加载播放（`instantiate(prefab)`、`Animation.play('anim_level_complete')`）。
   - 骨架仍是代码：需要精修的节点由代码创建后挂到约定挂点（如 `UICanvas/FXLayer`），或做成小型纯素材预制体；场景结构与业务逻辑不进编辑器，避免 UUID 引用损坏。
   - 协作闭环：AI 输出"编辑器操作单"（精确到节点路径、属性名、数值——时长、缓动曲线类型、起止值、色值、粒子参数）→ 人工照单在编辑器操作并保存提交 → AI 读取 `.prefab/.anim/.effect` 文本文件核对结果 → 代码接线 → 编辑器预览/真机截图回灌审美反馈。

3. **AI 三级迭代回路**
   | 层级 | 目标 | 方式 | 人工参与 |
   |---|---|---|---|
   | L1 逻辑层 | puzzle-kit 生成/求解/难度 | Vitest 单测，AI 全自动跑（无头环境） | 无 |
   | L2 视觉层 | 布局、动效、交互 | Cocos 同一工程构建 **Web 版**（`--build "platform=web-mobile"`），无头浏览器（Playwright）截图 + 控制台日志回灌给 AI 自查 | 偶尔人工看效果 |
   | L3 平台层 | 微信/抖音真机表现 | Cocos 命令行构建（`CocosCreator --project <path> --build "platform=wechatgame"`）→ 微信开发者工具自动化 SDK（miniprogram-automator，官方有小游戏版文档）截图验证 | 人工做真机体验 |
   - 人工介入点收敛为：真机手感、审美终审、开发者工具/后台操作（提审、流量主、广告位配置）。

4. **资产流水线（对应"人工辅助 AI 提供资源"）**
   - UI 素材/图标/粒子贴图：AI 生图产出初稿 → 人工做风格一致性筛选 → 统一色板/圆角/描边规范约束生成。
   - 音效/音乐：CC0 素材包或 AI 音乐生成，人工挑选。
   - 资源全部走目录 + 代码动态引用，图集自动合图；字体用数字字体子集化（数独只有 1–9，可做到几十 KB）。

5. **仓库纪律**
   - 锁定 Creator 版本、`.meta` 一并提交、`cc.config.json` 构建配置入库，脚本化"一条命令出 Web/微信/抖音三包"。
   - 编辑器里改完动画/预制体必须及时保存提交，并遵守命名规范，否则代码接线会断。

6. **流畅动画守则（代码与编辑器两侧共同遵守）**
   - 只动画 transform 属性（位置/缩放/旋转/透明度），不动布局属性（Widget/Layout 一动就触发全树重排，是掉帧首因）。
   - 动效元素放独立 FXLayer，同图集、同层级保持 UI 合批；彩色粒子控制在专用层，避免打断批处理。
   - 缓动曲线：交互反馈用 backOut/elastic 弹性系制造"juice"，转场用 smooth 曲线；编辑器里直接拖曲线预设。
   - 粒子防 overdraw：大面积半透明粒子是低端机掉帧元凶，控制发射面积与叠加层数。
   - 低端机降级开关：监测帧率，低于阈值自动关粒子/震屏，保逻辑流畅。
   - 验收标准：微信真机性能面板 60fps；每周末人工视觉验收清单过一遍。

### 8.3 诚实的成本提示

- **审美终审必须是人**：AI 能靠截图发现布局错误、动效断裂，但"好不好看、爽不爽"的最终评判靠你；建议每周固定一次视觉验收清单。
- 编辑器人工操作的占用项增加（动画精修、粒子调参、布局微调，预计每周数小时），换来的是动效上限与"所见即所得"的效率；AI 按操作单+核对文件的闭环尽量降低你的操作成本。
- 平台侧事务（备案、提审、流量主/穿山甲后台、广告位 ID 配置）无法由代码 AI 代劳，是人工的主要占用项。

### 8.4 备选路线（说明为什么不用）

Web Canvas 栈（PixiJS/Phaser）对 AI 最友好——纯代码、浏览器即跑、无编辑器。但小游戏无 DOM：ScrollView、弹窗、适配、粒子全要自建或依赖社区适配器，且各平台运行时版本升级有兼容风险。本方案的"混合模式 Cocos"既能保住 AI 自主性，又留住编辑器的效果上限，是最优平衡。

---

## 9. 参考来源

- [Cocos Creator 官方文档 · 发布到微信小游戏](https://docs.cocos.com/creator/4.0/manual/zh/editor/publish/publish-wechatgame.html)
- [Cocos Creator · 发布到抖音小游戏](https://github.com/cocos/cocos-docs/blob/master/versions/3.8/zh/editor/publish/publish-bytedance-mini-game.md)
- [微信官方 · 小游戏流量主开通流程（UV>500）](https://ad.weixin.qq.com/docs/237)
- [微信官方 · 分包加载（首包 4M / 总包 30M）](https://developers.weixin.qq.com/minigame/dev/guide/base-ability/subPackage/useSubPackage.html)
- [微信官方 · 小程序流量主运营规范](https://ad.weixin.qq.com/guide/1195)
- [抖音开放平台 · 广告小游戏运营规范（激励视频红线）](https://developer.open-douyin.com/docs/resource/zh-CN/mini-game/operation1/norms/norms)
- [知乎 · 个人可以上线抖音小游戏吗（软著/备案）](https://zhuanlan.zhihu.com/p/683107713)
- [Cocos · 头条/抖音小游戏流量、分成及接入指南](https://www.cocos.com/post/8606)
- [知乎 · 微信小游戏开发为什么 90% 选 Cocos](https://zhuanlan.zhihu.com/p/2020589771861305207)
- [Cocos Creator 3.8 · 命令行发布项目（AI 自动化构建的关键）](https://docs.cocos.com/creator/3.8/manual/zh/editor/publish/publish-in-command-line.html)
- [微信开放文档 · 小游戏自动化（miniprogram-automator 快速入门）](https://developers.weixin.qq.com/minigame/dev/devtools/auto/quick-start)
- [微信开放文档 · 开发者工具命令行调用](https://developers.weixin.qq.com/miniprogram/dev/devtools/cli.html)
